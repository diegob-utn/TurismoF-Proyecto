@model IEnumerable<TurismoF.Modelos.Vagon>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Numero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoVagon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EsPreferencial)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tren)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Numero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoVagon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EsPreferencial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tren.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
