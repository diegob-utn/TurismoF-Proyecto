@model IEnumerable<TurismoF.Modelos.Asiento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Codigo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoAsiento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumeroVagon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vagon)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Codigo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoAsiento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroVagon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vagon.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
